export enum DataPersistKeys {
    DescriptionFilterKey = 'filterDescription',
    DateFilterKey = 'filterDate',
}

export enum InputTypes {
    Text = 'text',
    Number = 'number',
    Password = 'password',
    Email = 'email',
    File = 'file',
    Date = 'date',
}

export enum FieldTypes {
    Input = 'input',
    Select = 'select',
    File = 'file',
    Date = 'date',
}

export enum FieldAutoCompleteValues {
    Off = 'off',
    On = 'on',
    NewPassword = 'new-password',
}

export enum ValidationErrorStyle {
    ValidationErrorUnderline = 'validation-error--underline',
    ValidationErrorTooltip = 'validation-error--tooltip',
}

export enum DateFormatTypes {
    DateOnly = 'P',
    TimeOnly = 'p',
    DateTime = 'Pp',
}

export enum TypeOfDataTypes {
    StringType = 'string',
    ObjectType = 'object',
    NumberType = 'number',
    BooleanType = 'boolean',
    FunctionType = 'function',
    BigintType = 'bigint',
    SymbolType = 'symbol',
    UndefinedType = 'undefined',
}
